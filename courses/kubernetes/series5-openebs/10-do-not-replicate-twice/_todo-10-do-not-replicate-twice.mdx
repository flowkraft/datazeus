---
title: "Double Replication — If the Database Already Does It, the Storage Should Not"
description: "TODO — one plain sentence saying what the reader will be able to do. Same rules as the title: no rhetorical devices, no jargon, about data."
topic: "learn-kubernetes"
tags: []
level: "advanced"
module: "series5-openebs"
order: 10
published: false
---

{/* ==========================================================================
    TODO — NOT A LESSON YET. A brief for whoever writes it, not teaching
    material. Named _todo-* so it stays out of the site build. Drop the prefix
    when it becomes real.

    READ FIRST
      courses/kubernetes/curriculum.yaml   the track's decisions, in its header
      tests/src/koans/groovy/datazeus/kubernetes/series5/_10/DoNotReplicateTwiceKoans.groovy

    ── FOLLOW THE OFFICIAL DOCUMENTATION ───────────────────────────────────
    THE COVERAGE, THE TOPICS AND THE ORDER OF THIS INDIVIDUAL LESSON — not only
    the series order — SHOULD FOLLOW
    the official Kubernetes documentation (kubernetes.io/docs) — the Concepts section that owns this topic (Workloads, Storage, Configuration, Scheduling and Eviction).
    Open its own section for this topic BEFORE writing, use it as the COVERAGE
    CHECKLIST, and use its VOCABULARY FOR THE NOUNS. Anything left out should be
    left out because an academy rule says so (Data Ops owns the single machine and the container image, Airflow owns dependencies between steps),
    and NEVER because it was missed.

    ── THE ACADEMY RULE THAT OUTRANKS EVERYTHING ELSE ──────────────────────
    A track teaches a SKILL and DEMONSTRATES it in a tool, never the reverse.
    Kubernetes assumes a workload is stateless and long-running. A data job is neither, and that mismatch is the reason for most surprises here.

    ── WHAT THIS LESSON SHOULD COVER (from curriculum.yaml) ────────────────
     THE ARGUMENT THIS SERIES EXISTS TO MAKE, and it is the most valuable episode in the three.
     CASSANDRA, CLICKHOUSE, KAFKA, ELASTICSEARCH AND MONGODB ALL REPLICATE THEMSELVES. Put one
     on replicated block storage and every write is replicated twice — once by the database
     for correctness and once by the storage for durability — paying network and latency for a
     guarantee you already had.
     THE RULE: IF THE DATABASE REPLICATES, THE STORAGE SHOULD NOT. Give it a fast local disk
     and let it do its job. NoSQL Concepts, Series 3 · 05 and Apache Cassandra, Series 2 · 00
     taught the replication factor; this is what it means for the volume underneath.
     AND THE OTHER HALF, SAID HONESTLY: recovery is now the DATABASE's job. A dead node means
     a Cassandra bootstrap, not a storage rebuild — which is usually fine and is a different
     runbook that somebody must know. If nobody does, replicated storage is buying you a
     safety net for an operational gap, and that is a legitimate reason to pay twice.

    ── HANDS-ON (from curriculum.yaml `hands_on`) ──────────────────────────
      koan:choose
      koan:predict
      koan file: tests/src/koans/groovy/datazeus/kubernetes/series5/_10/DoNotReplicateTwiceKoans.groovy
   ========================================================================== */}

## The situation this lesson prepares you for

{/* BEAT 1 — the concrete moment at work. Never a definition. */}

TODO

## The principle, then how this tool spells it

{/* BEAT 2 — the general idea first, then the syntax. A track teaches a SKILL and DEMONSTRATES it in a tool, never the reverse. */}

TODO

## See it for yourself

{/* BEAT 3 — NON-NEGOTIABLE. The hands-on above, or the reason there isn't one. */}

TODO

## What goes wrong

{/* BEAT 4 — the wrong number or silent failure this prevents, and how you would notice it. */}

TODO
