---
title: "nested and join — the Match Across Two Sub-Documents You Did Not Mean"
description: "TODO — one plain sentence saying what the reader will be able to do. Same rules as the title: no rhetorical devices, no jargon, about data."
topic: "learn-elasticsearch"
tags: []
level: "advanced"
module: "series2-aggregations-and-data"
order: 10
published: false
---

{/* ==========================================================================
    TODO — NOT A LESSON YET. A brief for whoever writes it, not teaching
    material. Named _todo-* so it stays out of the site build. Drop the prefix
    when it becomes real.

    READ FIRST
      courses/elasticsearch/curriculum.yaml   the track's decisions, in its header
      tests/src/koans/groovy/datazeus/elasticsearch/series2/_10/NestedAndJoinKoans.groovy

    ── FOLLOW THE OFFICIAL DOCUMENTATION ───────────────────────────────────
    THE COVERAGE, THE TOPICS AND THE ORDER OF THIS INDIVIDUAL LESSON — not only
    the series order — SHOULD FOLLOW
    the official Elasticsearch Guide — the page that owns this topic (mappings, query DSL, aggregations, index lifecycle, cluster sizing).
    Open its own section for this topic BEFORE writing, use it as the COVERAGE
    CHECKLIST, and use its VOCABULARY FOR THE NOUNS. Anything left out should be
    left out because an academy rule says so (NoSQL Concepts owns the search shape, PostgreSQL owns full-text inside a relational database, Data Ops owns the cluster),
    and NEVER because it was missed.

    ── THE ACADEMY RULE THAT OUTRANKS EVERYTHING ELSE ──────────────────────
    A track teaches a SKILL and DEMONSTRATES it in a tool, never the reverse.
    Search is not filtering, and this is not your source of truth. Both misunderstandings cost more the later they are learned.

    ── WHAT THIS LESSON SHOULD COVER (from curriculum.yaml) ────────────────
     COVER: the two ways to model a one-to-many inside a document, and their costs. NESTED,
     where each sub-document is indexed separately so a query cannot match across two of them
     — which is the whole point, and the flat alternative silently matches "red" from one
     shoe and "size 9" from another. JOIN fields (parent/child) for the case where children
     change far more often than parents.
     THE HONEST ADVICE: both are expensive and both are frequently a sign that the data was
     modelled for a database rather than for an index. DENORMALIZE FIRST — NoSQL Concepts,
     Series 1 · 10 — and reach for these only when duplication genuinely will not do.

    ── HANDS-ON (from curriculum.yaml `hands_on`) ──────────────────────────
      koan:diagnose
      koan:choose
      koan file: tests/src/koans/groovy/datazeus/elasticsearch/series2/_10/NestedAndJoinKoans.groovy
   ========================================================================== */}

## The situation this lesson prepares you for

{/* BEAT 1 — the concrete moment at work. Never a definition. */}

TODO

## The principle, then how this tool spells it

{/* BEAT 2 — the general idea first, then the syntax. A track teaches a SKILL and DEMONSTRATES it in a tool, never the reverse. */}

TODO

## See it for yourself

{/* BEAT 3 — NON-NEGOTIABLE. The hands-on above, or the reason there isn't one. */}

TODO

## What goes wrong

{/* BEAT 4 — the wrong number or silent failure this prevents, and how you would notice it. */}

TODO
