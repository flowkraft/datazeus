---
title: "Node, Property or Relationship — Three Tests, and the Supernode to Avoid"
description: "TODO — one plain sentence saying what the reader will be able to do. Same rules as the title: no rhetorical devices, no jargon, about data."
topic: "learn-neo4j"
tags: []
level: "advanced"
module: "series2-modelling-a-graph"
order: 0
published: false
---

{/* ==========================================================================
    TODO — NOT A LESSON YET. A brief for whoever writes it, not teaching
    material. Named _todo-* so it stays out of the site build. Drop the prefix
    when it becomes real.

    READ FIRST
      courses/neo4j/curriculum.yaml   the track's decisions, in its header
      tests/src/koans/groovy/datazeus/neo4j/series2/_00/WhatBecomesANodeKoans.groovy

    ── FOLLOW THE OFFICIAL DOCUMENTATION ───────────────────────────────────
    THE COVERAGE, THE TOPICS AND THE ORDER OF THIS INDIVIDUAL LESSON — not only
    the series order — SHOULD FOLLOW
    the official Neo4j documentation — the Cypher Manual page for that clause, the Graph Data Modeling guide, or the GDS library.
    Open its own section for this topic BEFORE writing, use it as the COVERAGE
    CHECKLIST, and use its VOCABULARY FOR THE NOUNS. Anything left out should be
    left out because an academy rule says so (NoSQL Concepts owns the graph shape, Learn SQL Series 3 - 00 owns the recursive CTE this must be measured against),
    and NEVER because it was missed.

    ── THE ACADEMY RULE THAT OUTRANKS EVERYTHING ELSE ──────────────────────
    A track teaches a SKILL and DEMONSTRATES it in a tool, never the reverse.
    The cost of a traversal grows with the size of the ANSWER, not the database. That is also its limit: two hops is a join.

    ── WHAT THIS LESSON SHOULD COVER (from curriculum.yaml) ────────────────
     COVER: the three decisions, each with a test rather than a preference.
       NODE OR PROPERTY?      make it a node when you want to traverse to it or ask questions
                              about it. A city you filter by is a property; a city you want
                              "who else lives here" for is a node.
       RELATIONSHIP OR NODE?  when a relationship needs its own relationships — an order
                              connecting a customer and products, with a date and a status —
                              it becomes an intermediate node. This is where join tables
                              reappear, and it is correct here.
       DIRECTION              store one direction and traverse either way; storing both is a
                              common and expensive mistake.
     SUPERNODES as the modelling problem with real consequences — a node with a million
     relationships makes every traversal through it slow — and the standard mitigations:
     relationship types that partition the traffic, intermediate nodes, or accepting it.

    ── HANDS-ON (from curriculum.yaml `hands_on`) ──────────────────────────
      koan:choose
      koan:predict
      koan file: tests/src/koans/groovy/datazeus/neo4j/series2/_00/WhatBecomesANodeKoans.groovy
   ========================================================================== */}

## The situation this lesson prepares you for

{/* BEAT 1 — the concrete moment at work. Never a definition. */}

TODO

## The principle, then how this tool spells it

{/* BEAT 2 — the general idea first, then the syntax. A track teaches a SKILL and DEMONSTRATES it in a tool, never the reverse. */}

TODO

## See it for yourself

{/* BEAT 3 — NON-NEGOTIABLE. The hands-on above, or the reason there isn't one. */}

TODO

## What goes wrong

{/* BEAT 4 — the wrong number or silent failure this prevents, and how you would notice it. */}

TODO
