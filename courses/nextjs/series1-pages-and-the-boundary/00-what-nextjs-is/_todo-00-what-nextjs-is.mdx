---
title: "A Page in Front of a Query — What This Buys Over a BI Dashboard, and When It Does Not"
description: "TODO — one plain sentence saying what the reader will be able to do. Same rules as the title: no rhetorical devices, no jargon, about data."
topic: "learn-nextjs"
tags: []
level: "intermediate"
module: "series1-pages-and-the-boundary"
order: 0
published: false
---

{/* ==========================================================================
    TODO — NOT A LESSON YET. A brief for whoever writes it, not teaching
    material. Named _todo-* so it stays out of the site build. Drop the prefix
    when it becomes real.

    READ FIRST
      courses/nextjs/curriculum.yaml   the track's decisions, in its header
      tests/src/koans/groovy/datazeus/nextjs/series1/_00/WhatNextjsIsKoans.groovy

    ── FOLLOW THE OFFICIAL DOCUMENTATION ───────────────────────────────────
    THE COVERAGE, THE TOPICS AND THE ORDER OF THIS INDIVIDUAL LESSON — not only
    the series order — SHOULD FOLLOW
    the official Next.js documentation (nextjs.org/docs) — the Getting Started page that owns this topic, App Router only.
    Open its own section for this topic BEFORE writing, use it as the COVERAGE
    CHECKLIST, and use its VOCABULARY FOR THE NOUNS. Anything left out should be
    left out because an academy rule says so (Learn SQL owns the query, BI & Data Visualization owns choosing the chart, Data Ops owns the host),
    and NEVER because it was missed.

    ── THE ACADEMY RULE THAT OUTRANKS EVERYTHING ELSE ──────────────────────
    A track teaches a SKILL and DEMONSTRATES it in a tool, never the reverse.
    Server by default. The boundary decides where the query runs — push it clientside and you have built a public API over your warehouse. CHECK CACHING CLAIMS AGAINST THE CURRENT VERSION; the defaults have moved.

    ── WHAT THIS LESSON SHOULD COVER (from curriculum.yaml) ────────────────
     COVER: what Next.js is — React plus a router plus a server — and the framing that keeps a
     data reader oriented: it is a way to put a rendered page in front of a query. Install,
     project structure, and the App Router as the only router this course teaches, with the
     Pages Router named once so existing code is recognisable.
     THE HONEST GATE GOES HERE, in full, not as a footnote: if a BI tool will do, use the BI
     tool. Build when the audience is external, the interaction is not a dashboard, or you
     are shipping a product. A course that never says when NOT to use its subject is an
     advertisement.

    ── HANDS-ON (from curriculum.yaml `hands_on`) ──────────────────────────
      koan:choose
      koan:capture
      koan file: tests/src/koans/groovy/datazeus/nextjs/series1/_00/WhatNextjsIsKoans.groovy
   ========================================================================== */}

## The situation this lesson prepares you for

{/* BEAT 1 — the concrete moment at work. Never a definition. */}

TODO

## The principle, then how this tool spells it

{/* BEAT 2 — the general idea first, then the syntax. A track teaches a SKILL and DEMONSTRATES it in a tool, never the reverse. */}

TODO

## See it for yourself

{/* BEAT 3 — NON-NEGOTIABLE. The hands-on above, or the reason there isn't one. */}

TODO

## What goes wrong

{/* BEAT 4 — the wrong number or silent failure this prevents, and how you would notice it. */}

TODO
