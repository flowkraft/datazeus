---
title: "The Four — What CockroachDB, YugabyteDB, TiDB and Spanner Each Chose"
description: "TODO — one plain sentence saying what the reader will be able to do. Same rules as the title: no rhetorical devices, no jargon, about data."
topic: "learn-distributed-sql"
tags: []
level: "advanced"
module: "series3-the-landscape-and-the-decision"
order: 0
published: false
---

{/* ==========================================================================
    TODO — NOT A LESSON YET. A brief for whoever writes it, not teaching
    material. Named _todo-* so it stays out of the site build. Drop the prefix
    when it becomes real.

    READ FIRST
      courses/distributedsql/curriculum.yaml   the track's decisions, in its header
      tests/src/koans/groovy/datazeus/distributedsql/series3/_00/TheFourKoans.groovy

    ── FOLLOW THE OFFICIAL DOCUMENTATION ───────────────────────────────────
    THE COVERAGE, THE TOPICS AND THE ORDER OF THIS INDIVIDUAL LESSON — not only
    the series order — SHOULD FOLLOW
    EACH PRODUCT'S OWN CURRENT DOCUMENTATION for every product named, with the version AND EDITION stated. THIS COURSE HAS NO MANUAL OF ITS OWN and teaches no product — if a beat needs something installed, it belongs in the CockroachDB track.
    Open its own section for this topic BEFORE writing, use it as the COVERAGE
    CHECKLIST, and use its VOCABULARY FOR THE NOUNS. Anything left out should be
    left out because an academy rule says so (CockroachDB owns the hands-on, Learn SQL owns the SQL, NoSQL Concepts owns the non-relational models, Data Ops owns the machine),
    and NEVER because it was missed.

    ── THE ACADEMY RULE THAT OUTRANKS EVERYTHING ELSE ──────────────────────
    A track teaches a SKILL and DEMONSTRATES it in a tool, never the reverse.
    A COMMIT IS A CONVERSATION BETWEEN MACHINES. Licences and editions move fast here — check pricing pages, not blog posts.

    ── WHAT THIS LESSON SHOULD COVER (from curriculum.yaml) ────────────────
     COVER: the four, fairly, each in terms of the compromise it chose rather than a ranking.
       COCKROACHDB   PostgreSQL wire protocol and dialect, its own storage and query layer.
                     This academy's CockroachDB track is the hands-on version.
       YUGABYTEDB    PostgreSQL compatibility taken further by reusing the upstream query
                     layer, with distributed storage underneath.
       TIDB          MySQL-compatible, and notable for pairing transactional storage with a
                     columnar replica — a different answer to this course's own analytics
                     exclusion.
       SPANNER       the origin of the category and the paper everyone cites; managed only,
                     using synchronised clocks where the others use more messages.
     THE TRANSFERABLE PART, which is the point of the episode: ranges, placement by key,
     consensus writes and retryable transactions are true of all four. Say that, instead of
     producing a scorecard that will be wrong within a year.
     ⚠ VERIFY EDITIONS, LICENCES AND WHAT IS ACTUALLY FREE, on the day, from each vendor's
     own pages.

    ── HANDS-ON (from curriculum.yaml `hands_on`) ──────────────────────────
      koan:choose
      koan:capture
      koan file: tests/src/koans/groovy/datazeus/distributedsql/series3/_00/TheFourKoans.groovy
   ========================================================================== */}

## The situation this lesson prepares you for

{/* BEAT 1 — the concrete moment at work. Never a definition. */}

TODO

## The principle, then how this tool spells it

{/* BEAT 2 — the general idea first, then the syntax. A track teaches a SKILL and DEMONSTRATES it in a tool, never the reverse. */}

TODO

## See it for yourself

{/* BEAT 3 — NON-NEGOTIABLE. The hands-on above, or the reason there isn't one. */}

TODO

## What goes wrong

{/* BEAT 4 — the wrong number or silent failure this prevents, and how you would notice it. */}

TODO
