---
title: "pg_dump and pg_restore — Taking a Copy You Can Actually Restore"
description: "TODO — one plain sentence saying what the reader will be able to do. Same rules as the title: no rhetorical devices, no jargon, about data."
topic: "learn-postgresql"
tags: []
level: "intermediate"
module: "series1-postgres-essentials"
order: 37
published: false
---

{/* ==========================================================================
    TODO — NOT A LESSON YET. A brief for whoever writes it, not teaching
    material. Named _todo-* so it stays out of the site build. Drop the prefix
    when it becomes real.

    READ FIRST
      courses/postgresql/curriculum.yaml   the track's decisions, in its header
      tests/src/koans/groovy/datazeus/postgresql/series1/_37/PgDumpAndRestoreKoans.groovy

    ── FOLLOW THE OFFICIAL DOCUMENTATION ───────────────────────────────────
    THE COVERAGE, THE TOPICS AND THE ORDER OF THIS INDIVIDUAL LESSON — not only
    the series order — SHOULD FOLLOW
    the official PostgreSQL documentation (postgresql.org/docs).
    Open its own section for this topic BEFORE writing, use it as the COVERAGE
    CHECKLIST, and use its VOCABULARY FOR THE NOUNS. Anything left out should be
    left out because an academy rule says so (Learn SQL owns the SQL, Data Ops owns administration),
    and NEVER because it was missed.

    ── THE ACADEMY RULE THAT OUTRANKS EVERYTHING ELSE ──────────────────────
    A track teaches a SKILL and DEMONSTRATES it in a tool, never the reverse.
    This is a DELTA track: every sentence should be one that would be FALSE on another engine.

    ── WHAT THIS LESSON SHOULD COVER (from curriculum.yaml) ────────────────
     ADDED 2026-09-02. Backups were excluded to Data Ops, which is correct for the DISCIPLINE
     — that series is PostgreSQL and teaches PITR at Series 2 · 30 — but it left this course
     without the tool a developer types weekly. pg_dump is not an operations command; it is
     how you take a copy of a schema to your laptop.
     COVER: pg_dump's formats, and why CUSTOM or DIRECTORY rather than plain SQL — selective
     restore, parallel restore, and reordering. pg_restore with -j, --schema-only,
     --data-only, -t. pg_basebackup and WAL archiving NAMED as the physical alternative,
     with the detail left to Data Ops.
     THE TRAP THAT COSTS PEOPLE A RESTORE: pg_dump is PER DATABASE and does not include
     ROLES. Restore it somewhere new and every GRANT refers to a role that does not exist.
     pg_dumpall --globals-only is the missing half, and almost nobody runs it until the
     first real restore.

    ── HANDS-ON (from curriculum.yaml `hands_on`) ──────────────────────────
      koan:process:tool
      koan:diagnose
      koan file: tests/src/koans/groovy/datazeus/postgresql/series1/_37/PgDumpAndRestoreKoans.groovy
   ========================================================================== */}

## The situation this lesson prepares you for

{/* BEAT 1 — the concrete moment at work. Never a definition. */}

TODO

## The principle, then how this tool spells it

{/* BEAT 2 — the general idea first, then the syntax. A track teaches a SKILL and DEMONSTRATES it in a tool, never the reverse. */}

TODO

## See it for yourself

{/* BEAT 3 — NON-NEGOTIABLE. The hands-on above, or the reason there isn't one. */}

TODO

## What goes wrong

{/* BEAT 4 — the wrong number or silent failure this prevents, and how you would notice it. */}

TODO
