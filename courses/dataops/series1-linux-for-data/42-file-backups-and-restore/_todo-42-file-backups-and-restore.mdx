---
title: "File Backups — tar, rsync Snapshots, Rotation, and Proving a Restore Works"
description: "TODO — one plain sentence saying what the reader will be able to do. Same rules as the title: no rhetorical devices, no jargon, about data."
topic: "learn-data-ops"
tags: []
level: "beginner"
module: "series1-linux-for-data"
order: 42
published: false
---

{/* ==========================================================================
    TODO — NOT A LESSON YET. A brief for whoever writes it, not teaching
    material. Named _todo-* so it stays out of the site build. Drop the prefix
    when it becomes real.

    READ FIRST
      courses/dataops/curriculum.yaml   the track's decisions, in its header
      tests/src/koans/groovy/datazeus/dataops/series1/_42/FileBackupsAndRestoreKoans.groovy

    ── THE RULE THAT OUTRANKS EVERYTHING ELSE HERE ─────────────────────────
    This track teaches a SKILL and DEMONSTRATES it in a tool. Never the reverse.
    Teach the principle first, then show it: "here is how you do that in <tool>".
    Whatever the reader learns must still be true away from our tools — if this
    episode is only true inside DataPallas, it is product documentation and
    belongs in content/docs, not here.

    ── WHERE THIS SITS ─────────────────────────────────────────────────────
    Data Ops · Linux for Data (beginner) — "do real data work from the terminal, with shell tools alone"
      before:  cron & systemd Timers — Turn a Script into a Nightly Job
      THIS:    File Backups — tar, rsync Snapshots, Rotation, and Proving a Restore Works
      after:   Git — Version Control for the Scripts and Config That Run Your Data
    CORE — on the shortest path to being productive. Someone will read only
    the core episodes, so this one has to stand alone.

    ── WHY THIS EPISODE, SPECIFICALLY ──────────────────────────────────────
    GOAL: back up the parts of a data box that are NOT the database — scripts, config, the
    raw landing zone, the crontab — on a schedule, with a retention you can state out loud,
    and then prove the backup restores.
    DECISION: it exists because a data box is not only its database, and this track had no
    answer for the rest of it. It sits HERE, immediately after cron, because the previous
    episode just taught the reader to schedule a script and this is the first script most
    people schedule. Any earlier and there is no scheduler to hang it on.

    ── COVER (the full argument lives in curriculum.yaml, above this episode) ─
      WHAT IS WORTH BACKING UP  scripts and config, the raw landing zone, the crontab —
                                and NOT what you can regenerate.
      THE TOOLS                 tar for a point-in-time archive; rsync --link-dest for
                                cheap incremental snapshots that each look like a full
                                copy; one line on filesystem and cloud snapshots possibly
                                doing this better than you can.
      ROTATION                  keeping every backup forever is the same bug as keeping
                                none — the disk fills and then nothing is backed up.
      THE 3-2-1 IDEA            one line, no ceremony: a copy on the same disk is not a
                                backup.

    THE BEAT THAT MAKES IT AN EPISODE RATHER THAN A CHECKLIST, and it is the same
    discipline Series 2 · 30 applies to pg_dump: A BACKUP YOU HAVE NEVER RESTORED IS A
    GUESS. Restore into a scratch directory and diff it.

    SEAM: Series 2 · 30 is the DATABASE's own backup, and Series 3 · 35 owns retention as
    a capacity question. This is the filesystem, and it repeats neither.

    ── HANDS-ON (from curriculum.yaml) ─────────────────────────────────────
      koan:process:tool
      koan:diagnose
      koan file: tests/src/koans/groovy/datazeus/dataops/series1/_42/FileBackupsAndRestoreKoans.groovy
   ========================================================================== */}

## The situation this lesson prepares you for

{/* BEAT 1 — the concrete moment at work. Never a definition. */}

TODO

## TODO — the principle

{/* BEAT 2 — the general idea, true anywhere. Then, and only then, how it is
    done in the tool we demonstrate in. */}

TODO

## TODO — see it for yourself

{/* BEAT 3 — NON-NEGOTIABLE. The koans above, or the reason there isn't one. */}

TODO

## What goes wrong

{/* BEAT 4 — the wrong number this prevents, and how you would notice it.
    The one to write: a cron backup whose destination filled up in March. It has been
    "running" ever since — exit code 0 on a partial copy — and the mail nobody reads went
    to a local mbox. Check that the backup GREW, not that the job ran. */}

TODO
