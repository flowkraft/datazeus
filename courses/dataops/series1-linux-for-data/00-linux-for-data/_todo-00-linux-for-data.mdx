---
title: "Linux — Why Data Work Lives on This OS, and at a Prompt"
description: "TODO — one plain sentence saying what the reader will be able to do. Same rules as the title: no rhetorical devices, no jargon, about data."
topic: "learn-data-ops"
tags: []
level: "beginner"
module: "series1-linux-for-data"
order: 00
published: false
---

{/* ==========================================================================
    TODO — NOT A LESSON YET. A brief for whoever writes it, not teaching
    material. Named _todo-* so it stays out of the site build. Drop the prefix
    when it becomes real.

    READ FIRST
      courses/dataops/curriculum.yaml   the track's decisions, in its header
      tests/src/koans/groovy/datazeus/dataops/series1/_00/LinuxForDataKoans.groovy

    ── THE RULE THAT OUTRANKS EVERYTHING ELSE HERE ─────────────────────────
    This track teaches a SKILL and DEMONSTRATES it in a tool. Never the reverse.
    Teach the principle first, then show it: "here is how you do that in <tool>".
    Whatever the reader learns must still be true away from our tools — if this
    episode is only true inside DataPallas, it is product documentation and
    belongs in content/docs, not here.

    ── WHERE THIS SITS ─────────────────────────────────────────────────────
    Data Ops · Linux for Data (beginner) — "do real data work from the terminal, with shell tools alone"
      before:  (first in the series)
      THIS:    Linux — Why Data Work Lives on This OS, and at a Prompt
      after:   The File System — Paths, ls, cp, mv, find and Where Your Data Lives
    CORE — on the shortest path to being productive. Someone will read only
    the core episodes, so this one has to stand alone.

    ── WHY THIS EPISODE, SPECIFICALLY ──────────────────────────────────────
    GOAL: say why the machines your data lives on run this OS, and why the answer you keep is a line rather than a sequence of clicks.
    DECISION: BROADENED 2026-09-03. This episode argued for the SHELL and never once said the word Linux — a series called Linux for Data that opened without introducing Linux. It is one episode and not two, because it is one argument: the reason data work lives on this operating system is the reason it lives at a prompt.

    ── COVER (the full argument lives in curriculum.yaml, above this episode) ─
      WHY THIS OS                it is what the servers are. Your database, your scheduler and every
                                 container in the catalogue run on Linux whatever you develop on. Give
                                 the practical shape of that for a Windows or Mac reader — WSL, a VM, a
                                 container — in three sentences, not a setup guide.
      EVERYTHING IS A FILE       the idea the rest of the series is built on. An extract, a device, a
                                 socket and a program's output are all a stream of bytes with a name,
                                 which is WHY tiny tools compose at all.
      TEXT AS THE INTERFACE      programs speak lines, so anything can feed anything. A GUI does not have
                                 this property and cannot be given it.
      WHY A PROMPT               the original argument, kept: one pipe answers a data question faster
                                 than a GUI — and, the part that outlasts speed, the answer is a LINE YOU
                                 CAN KEEP. Rerun it, schedule it, commit it, send it to someone.
      THE DISTROS                one paragraph. Debian/Ubuntu against RHEL/Rocky/Alma, apt against dnf,
                                 and the assurance that this series is true on all of them. Then stop.
      WHAT FOLLOWS               say it, so the series reads as designed: the next episodes are the
                                 command families, one each — the file system (05), the machine itself
                                 (07), reading a file (10), composing tools (15), slicing text (20),
                                 rewriting streams (25), other machines (30), permissions (35).

    ── HANDS-ON (from curriculum.yaml) ─────────────────────────────────────
      koan:capture
      terminal
      koan file: tests/src/koans/groovy/datazeus/dataops/series1/_00/LinuxForDataKoans.groovy
   ========================================================================== */}

## The situation this lesson prepares you for

{/* BEAT 1 — the concrete moment at work. Never a definition. */}

TODO

## TODO — the principle

{/* BEAT 2 — the general idea, true anywhere. Then, and only then, how it is
    done in the tool we demonstrate in. */}

TODO

## TODO — see it for yourself

{/* BEAT 3 — NON-NEGOTIABLE. The koans above, or the reason there isn't one. */}

TODO

## What goes wrong

{/* BEAT 4 — the wrong number this prevents, and how you would notice it.
    The one to write: nothing goes wrong in this one — it is the orientation episode. Use BEAT 4 for the HONEST LIMIT instead: a GUI is better for looking at something once. This series is about the second time you need the same answer. */}

TODO
