---
title: "The File System — Paths, ls, cp, mv, find and Where Your Data Lives"
description: "TODO — one plain sentence saying what the reader will be able to do. Same rules as the title: no rhetorical devices, no jargon, about data."
topic: "learn-data-ops"
tags: []
level: "beginner"
module: "series1-linux-for-data"
order: 05
published: false
---

{/* ==========================================================================
    TODO — NOT A LESSON YET. A brief for whoever writes it, not teaching
    material. Named _todo-* so it stays out of the site build. Drop the prefix
    when it becomes real.

    READ FIRST
      courses/dataops/curriculum.yaml   the track's decisions, in its header
      tests/src/koans/groovy/datazeus/dataops/series1/_05/FilesAndNavigationKoans.groovy

    ── THE RULE THAT OUTRANKS EVERYTHING ELSE HERE ─────────────────────────
    This track teaches a SKILL and DEMONSTRATES it in a tool. Never the reverse.
    Teach the principle first, then show it: "here is how you do that in <tool>".
    Whatever the reader learns must still be true away from our tools — if this
    episode is only true inside DataPallas, it is product documentation and
    belongs in content/docs, not here.

    ── WHERE THIS SITS ─────────────────────────────────────────────────────
    Data Ops · Linux for Data (beginner) — "do real data work from the terminal, with shell tools alone"
      before:  Linux — Why Data Work Lives on This OS, and at a Prompt
      THIS:    The File System — Paths, ls, cp, mv, find and Where Your Data Lives
      after:   ps, df, systemctl & journalctl — Reading the Machine Your Job Runs On
    CORE — on the shortest path to being productive. Someone will read only
    the core episodes, so this one has to stand alone.

    ── WHY THIS EPISODE, SPECIFICALLY ──────────────────────────────────────
    GOAL: move around a machine you have never seen, find the file you need by name, age or size, and copy or move it without breaking anything.
    DECISION: the FILE SYSTEM command family, and the first of the families that make up this series. The title said "Navigation", which had been reading as ls and cd alone; the family is larger than that and find is most of its value.

    ── COVER (the full argument lives in curriculum.yaml, above this episode) ─
      THE SHAPE OF THE TREE      / , /etc, /var/log, /tmp, /home, /mnt — first, because a reader who
                                 knows where things live stops guessing.
      PATHS                      absolute against relative, . and .. and ~.
      THE FAMILY                 ls -lh, cd, pwd, mkdir -p, cp -r, mv, rm — and rm -rf with the respect
                                 it deserves — ln -s for the symlinks an ops box is full of, and file
                                 plus stat for what something actually is.
      FIND EARNS A THIRD OF THE EPISODE find . -name, -mtime, -size, -exec. It answers "which extract is
                                        missing" and "what has not been written since Tuesday", and a data
                                        person uses it weekly.

    ── HANDS-ON (from curriculum.yaml) ─────────────────────────────────────
      koan:complete
      terminal
      koan file: tests/src/koans/groovy/datazeus/dataops/series1/_05/FilesAndNavigationKoans.groovy
   ========================================================================== */}

## The situation this lesson prepares you for

{/* BEAT 1 — the concrete moment at work. Never a definition. */}

TODO

## TODO — the principle

{/* BEAT 2 — the general idea, true anywhere. Then, and only then, how it is
    done in the tool we demonstrate in. */}

TODO

## TODO — see it for yourself

{/* BEAT 3 — NON-NEGOTIABLE. The koans above, or the reason there isn't one. */}

TODO

## What goes wrong

{/* BEAT 4 — the wrong number this prevents, and how you would notice it.
    The one to write: a filename with a space — or a newline — in it. It arrives with every CSV somebody exported from a spreadsheet, and it breaks every unquoted loop written after it. */}

TODO
