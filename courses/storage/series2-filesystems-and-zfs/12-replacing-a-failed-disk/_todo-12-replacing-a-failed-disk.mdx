---
title: "zpool replace — the Command for the Thing That Will Actually Happen"
description: "TODO — one plain sentence saying what the reader will be able to do. Same rules as the title: no rhetorical devices, no jargon, about data."
topic: "learn-storage"
tags: []
level: "advanced"
module: "series2-filesystems-and-zfs"
order: 12
published: false
---

{/* ==========================================================================
    TODO — NOT A LESSON YET. A brief for whoever writes it, not teaching
    material. Named _todo-* so it stays out of the site build. Drop the prefix
    when it becomes real.

    READ FIRST
      courses/storage/curriculum.yaml   the track's decisions, in its header
      tests/src/koans/groovy/datazeus/storage/series2/_12/ReplacingAFailedDiskKoans.groovy

    ── FOLLOW THE OFFICIAL DOCUMENTATION ───────────────────────────────────
    THE COVERAGE, THE TOPICS AND THE ORDER OF THIS INDIVIDUAL LESSON — not only
    the series order — SHOULD FOLLOW
    the project that owns the series — OpenZFS docs for Series 2, the S3 API reference and MinIO's docs for Series 3, the Ceph docs for Series 4. SERIES 1 HAS NO MANUAL: it is physics, arithmetic and trade-offs, so teach the SHAPE and the orders of magnitude.
    Open its own section for this topic BEFORE writing, use it as the COVERAGE
    CHECKLIST, and use its VOCABULARY FOR THE NOUNS. Anything left out should be
    left out because an academy rule says so (Data Ops owns renting a bucket and watching a disk, Data Warehousing owns columnar layout, the engine tracks own their storage engines),
    and NEVER because it was missed.

    ── THE ACADEMY RULE THAT OUTRANKS EVERYTHING ELSE ──────────────────────
    A track teaches a SKILL and DEMONSTRATES it in a tool, never the reverse.
    REPLICATION IS NOT A BACKUP — a delete is replicated faithfully to all three copies. DATE EVERY CONCRETE FIGURE: capacities, prices and rebuild times age faster here than anywhere else in the academy.

    ── WHAT THIS LESSON SHOULD COVER (from curriculum.yaml) ────────────────
     COVER: copy-on-write and what it buys — SNAPSHOTS THAT COST ALMOST NOTHING because
     nothing is copied until it changes, clones for a writable copy, and send/receive for
     replication to another machine.
     WHY A DATA TEAM CARES: a snapshot before a migration, a clone as a test database with
     production-shaped data, and incremental send as an offsite copy. Data Ops, Series 1 · 42
     said a copy on the same disk is not a backup, and send/receive is how that copy leaves.
     AND THE WARNING THAT COMES WITH COPY-ON-WRITE: snapshots hold space, so a pool can fill
     because of what you deleted. That is Series 1 · 25's cliff arriving from an unexpected
     direction.
     ADDED to close a proven gap: a review tested this course against what deploying and
     RUNNING each thing requires, and `zpool replace` appeared nowhere. 10 teaches that ZFS
     detects and repairs corruption, 1 · 20 teaches rebuilds as a concept — and nothing told
     the reader the command for the single most likely event in a pool's life.
     COVER, as a runbook: notice it (zpool status, and what DEGRADED reads like), identify the
     disk by a stable name rather than by /dev/sdb which moves, physically swap it, then
     `zpool replace`, then watch the resilver. WITH THE OUTPUT SHOWN AT EACH STEP.
     THE DETAIL THAT SAVES A POOL: use /dev/disk/by-id, not /dev/sdX. Kernel device names are
     not stable across reboots, and replacing the wrong disk in a degraded pool is how a
     single failure becomes a data-loss event.
     HOT SPARES and autoreplace in a paragraph. QUOTAS AND RESERVATIONS here too — also
     absent before this review — because "one dataset ate the pool" is the other thing that
     actually happens, and Series 1 · 25's cliff is where it ends.

    ── HANDS-ON (from curriculum.yaml `hands_on`) ──────────────────────────
      koan:process:tool
      koan:diagnose
      koan file: tests/src/koans/groovy/datazeus/storage/series2/_12/ReplacingAFailedDiskKoans.groovy
   ========================================================================== */}

## The situation this lesson prepares you for

{/* BEAT 1 — the concrete moment at work. Never a definition. */}

TODO

## The principle, then how this tool spells it

{/* BEAT 2 — the general idea first, then the syntax. A track teaches a SKILL and DEMONSTRATES it in a tool, never the reverse. */}

TODO

## See it for yourself

{/* BEAT 3 — NON-NEGOTIABLE. The hands-on above, or the reason there isn't one. */}

TODO

## What goes wrong

{/* BEAT 4 — the wrong number or silent failure this prevents, and how you would notice it. */}

TODO
