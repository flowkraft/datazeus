---
title: "ZFS — What It Is, Why It Exists, and When It Earns Its Place"
description: "TODO — one plain sentence saying what the reader will be able to do. Same rules as the title: no rhetorical devices, no jargon, about data."
topic: "learn-storage"
tags: []
level: "advanced"
module: "series2-filesystems-and-zfs"
order: 2
published: false
---

{/* ==========================================================================
    TODO — NOT A LESSON YET. A brief for whoever writes it, not teaching
    material. Named _todo-* so it stays out of the site build. Drop the prefix
    when it becomes real.

    READ FIRST
      courses/storage/curriculum.yaml   the track's decisions, in its header
      tests/src/koans/groovy/datazeus/storage/series2/_02/WhatZfsIsKoans.groovy

    ── FOLLOW THE OFFICIAL DOCUMENTATION ───────────────────────────────────
    THE COVERAGE, THE TOPICS AND THE ORDER OF THIS INDIVIDUAL LESSON — not only
    the series order — SHOULD FOLLOW
    the project that owns the series — OpenZFS docs for Series 2, the S3 API reference and MinIO's docs for Series 3, the Ceph docs for Series 4. SERIES 1 HAS NO MANUAL: it is physics, arithmetic and trade-offs, so teach the SHAPE and the orders of magnitude.
    Open its own section for this topic BEFORE writing, use it as the COVERAGE
    CHECKLIST, and use its VOCABULARY FOR THE NOUNS. Anything left out should be
    left out because an academy rule says so (Data Ops owns renting a bucket and watching a disk, Data Warehousing owns columnar layout, the engine tracks own their storage engines),
    and NEVER because it was missed.

    ── THE ACADEMY RULE THAT OUTRANKS EVERYTHING ELSE ──────────────────────
    A track teaches a SKILL and DEMONSTRATES it in a tool, never the reverse.
    REPLICATION IS NOT A BACKUP — a delete is replicated faithfully to all three copies. DATE EVERY CONCRETE FIGURE: capacities, prices and rebuild times age faster here than anywhere else in the academy.

    ── WHAT THIS LESSON SHOULD COVER (from curriculum.yaml) ────────────────
     COVER: ZFS's model, in its own vocabulary because the docs use it and nothing else does:
     POOL, VDEV, DATASET and ZVOL. The structural fact that surprises everybody: REDUNDANCY
     LIVES IN THE VDEV, and a pool is only as redundant as its weakest vdev — so adding a
     single disk to a mirrored pool quietly destroys the guarantee.
     MIRRORS AGAINST RAIDZ, with the trade stated: mirrors resilver fast and cost half the
     capacity, RAIDZ stores more and rebuilds slowly. Series 1 · 20 is why that matters.
     ADDED after a review asked whether a reader would finish knowing WHAT ZFS IS AND WHY.
     00 is filesystems in general and 05 dives straight into vdevs — the mechanism arrived
     before the motivation, which is the wrong order for somebody deciding.
     THE FIVE ANSWERS, and this episode owes four of them:
       WHAT IT IS      a filesystem and a volume manager IN ONE. That is the unusual part —
                       it owns the disks rather than sitting on somebody else's RAID, which
                       is what lets it do everything else.
       WHY IT EXISTS   because a filesystem that does not own the redundancy cannot repair
                       what it detects. 10 is the proof; this is the claim.
       WHEN IT EARNS ITS PLACE   a machine with several disks you control; data you cannot
                       silently corrupt; snapshots you want to be free; a database or
                       warehouse where a pre-load snapshot costs nothing and saves a night.
                       FOR A WAREHOUSE SPECIFICALLY, say it plainly: snapshot before the
                       nightly load, roll back in seconds if it goes wrong, and compression
                       usually makes the scans FASTER because fewer bytes leave the disk.
       WHEN IT DOES NOT   one virtual disk on a rented VM — use ext4 and read 20's gate.
     ON DEBIAN, CONCRETELY: the packages are in contrib, the module is built by DKMS against
     your running kernel, and that build is where people get stuck. Give the exact apt lines
     and what a successful `modprobe zfs` looks like. One line on RHEL-family and stop.

    ── HANDS-ON (from curriculum.yaml `hands_on`) ──────────────────────────
      koan:choose
      koan:process:tool
      koan file: tests/src/koans/groovy/datazeus/storage/series2/_02/WhatZfsIsKoans.groovy
   ========================================================================== */}

## The situation this lesson prepares you for

{/* BEAT 1 — the concrete moment at work. Never a definition. */}

TODO

## The principle, then how this tool spells it

{/* BEAT 2 — the general idea first, then the syntax. A track teaches a SKILL and DEMONSTRATES it in a tool, never the reverse. */}

TODO

## See it for yourself

{/* BEAT 3 — NON-NEGOTIABLE. The hands-on above, or the reason there isn't one. */}

TODO

## What goes wrong

{/* BEAT 4 — the wrong number or silent failure this prevents, and how you would notice it. */}

TODO
