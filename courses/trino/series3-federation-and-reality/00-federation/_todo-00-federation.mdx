---
title: "One Query, Several Systems — the Party Trick, and Who Pays For It"
description: "TODO — one plain sentence saying what the reader will be able to do. Same rules as the title: no rhetorical devices, no jargon, about data."
topic: "learn-trino"
tags: []
level: "advanced"
module: "series3-federation-and-reality"
order: 0
published: false
---

{/* ==========================================================================
    TODO — NOT A LESSON YET. A brief for whoever writes it, not teaching
    material. Named _todo-* so it stays out of the site build. Drop the prefix
    when it becomes real.

    READ FIRST
      courses/trino/curriculum.yaml   the track's decisions, in its header
      tests/src/koans/groovy/datazeus/trino/series3/_00/FederationKoans.groovy

    ── FOLLOW THE OFFICIAL DOCUMENTATION ───────────────────────────────────
    THE COVERAGE, THE TOPICS AND THE ORDER OF THIS INDIVIDUAL LESSON — not only
    the series order — SHOULD FOLLOW
    the official Trino documentation (trino.io/docs) — the page that owns this topic, and above all THE CONNECTOR'S OWN PAGE, because connectors differ enormously in what they push down.
    Open its own section for this topic BEFORE writing, use it as the COVERAGE
    CHECKLIST, and use its VOCABULARY FOR THE NOUNS. Anything left out should be
    left out because an academy rule says so (Learn SQL owns the SQL, Apache Iceberg owns the table format, Data Warehousing owns the lakehouse, Data Ops owns the cluster),
    and NEVER because it was missed.

    ── THE ACADEMY RULE THAT OUTRANKS EVERYTHING ELSE ──────────────────────
    A track teaches a SKILL and DEMONSTRATES it in a tool, never the reverse.
    TRINO HAS NO STORAGE. Every performance question is a question about somebody else's data layout. Say WHICH CONNECTOR each claim was verified against, and check whether a source is describing Trino or PrestoDB.

    ── WHAT THIS LESSON SHOULD COVER (from curriculum.yaml) ────────────────
     COVER: the party trick — one SELECT joining a table in object storage to a table in
     PostgreSQL to a topic in Kafka, with no copying and no pipeline. Demonstrate it, because
     it is genuinely impressive and it is why people adopt this.
     THEN THE BILL, immediately, in the same episode: the join happens IN TRINO unless the
     connector pushed it down, and it almost never can across two systems. So the remote rows
     cross the network, the planner has no statistics for them, and your analytical query is
     now running against somebody's production OLTP database at a time you chose.
     THE RULE, which is Data Warehousing, Series 3 · 22's rule arriving with a mechanism:
     FEDERATE WHAT IS SMALL, OCCASIONAL OR REFERENCE. Copy what is large, hot or joined in
     every query. Exploration and reconciliation are the honest use; a daily dashboard is not.

    ── HANDS-ON (from curriculum.yaml `hands_on`) ──────────────────────────
      koan:diagnose
      koan:choose
      koan file: tests/src/koans/groovy/datazeus/trino/series3/_00/FederationKoans.groovy
   ========================================================================== */}

## The situation this lesson prepares you for

{/* BEAT 1 — the concrete moment at work. Never a definition. */}

TODO

## The principle, then how this tool spells it

{/* BEAT 2 — the general idea first, then the syntax. A track teaches a SKILL and DEMONSTRATES it in a tool, never the reverse. */}

TODO

## See it for yourself

{/* BEAT 3 — NON-NEGOTIABLE. The hands-on above, or the reason there isn't one. */}

TODO

## What goes wrong

{/* BEAT 4 — the wrong number or silent failure this prevents, and how you would notice it. */}

TODO
