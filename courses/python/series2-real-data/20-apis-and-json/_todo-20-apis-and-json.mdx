---
title: "HTTP APIs & JSON — Fetching Data With Pagination, Retries and Rate Limits"
description: "TODO — one plain sentence saying what the reader will be able to do. Same rules as the title: no rhetorical devices, no jargon, about data."
topic: "learn-python"
tags: []
level: "intermediate"
module: "series2-real-data"
order: 20
published: false
---

{/* ==========================================================================
    TODO — NOT A LESSON YET. A brief for whoever writes it, not teaching
    material. Named _todo-* so it stays out of the site build. Drop the prefix
    when it becomes real.

    READ FIRST
      courses/python/curriculum.yaml       the track's decisions
      datasets/README.md                   the three dataset tiers, and why
      tests/src/koans/python/conftest.py   the koan rules and helpers

    HOW THIS TRACK IS HANDS-ON
      Jupyter is the EXPLORE surface, koans are the GRADED surface — exactly the
      split Learn SQL has between CloudBeaver and its koans. DataPallas ships
      JupyterLab; the koans run in Docker so nobody installs Python.

    ── WHY THIS EPISODE, SPECIFICALLY ──────────────────────────────────────
    GOAL: fetch data you do not own, completely and repeatably.
    LIBRARIES: httpx or requests, json, and a local stub server for the koans.
    DATASET: api-stub — python's own http.server in a fixture, so the koans need no network. A course
    that stops working on a train is a course that stops working.
    COUNTER-KOAN: count the ATTEMPTS. "Retried forever" and "gave up immediately" are both wrong and
    look identical from outside; the count separates them. Pagination: assert you got every page.

    ── HANDS-ON (from curriculum.yaml) ─────────────────────────────────────
      koan:complete
      koan:count:queries
      dataset: api-stub
      koan file: tests/src/koans/python/series2/_20/test_apis_and_json.py
   ========================================================================== */}

## The situation this lesson prepares you for

{/* BEAT 1 — the concrete moment at work. Never a definition. */}

TODO

## TODO — the code

{/* BEAT 2 — the libraries, one at a time, each with something the reader types.
    Show it in a notebook cell where a notebook is the honest place for it. */}

TODO

## TODO — see it for yourself

{/* BEAT 3 — NON-NEGOTIABLE. The koans above, or the reason there isn't one. */}

TODO

## What goes wrong

{/* BEAT 4 — the wrong number this prevents, and how you would notice it. */}

TODO
