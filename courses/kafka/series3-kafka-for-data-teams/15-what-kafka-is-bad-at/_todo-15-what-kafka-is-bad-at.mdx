---
title: "What Kafka Is Bad At — and the Table With a Timestamp That Would Have Done"
description: "TODO — one plain sentence saying what the reader will be able to do. Same rules as the title: no rhetorical devices, no jargon, about data."
topic: "learn-kafka"
tags: []
level: "advanced"
module: "series3-kafka-for-data-teams"
order: 15
published: false
---

{/* ==========================================================================
    TODO — NOT A LESSON YET. A brief for whoever writes it, not teaching
    material. Named _todo-* so it stays out of the site build. Drop the prefix
    when it becomes real.

    READ FIRST
      courses/kafka/curriculum.yaml   the track's decisions, in its header
      tests/src/koans/groovy/datazeus/kafka/series3/_15/WhatKafkaIsBadAtKoans.groovy

    ── FOLLOW THE OFFICIAL DOCUMENTATION ───────────────────────────────────
    THE COVERAGE, THE TOPICS AND THE ORDER OF THIS INDIVIDUAL LESSON — not only
    the series order — SHOULD FOLLOW
    the official Apache Kafka documentation (kafka.apache.org/documentation) — its Design section especially.
    Open its own section for this topic BEFORE writing, use it as the COVERAGE
    CHECKLIST, and use its VOCABULARY FOR THE NOUNS. Anything left out should be
    left out because an academy rule says so (ETL owns CDC and dead letters as concepts, Data Ops owns running the brokers),
    and NEVER because it was missed.

    ── THE ACADEMY RULE THAT OUTRANKS EVERYTHING ELSE ──────────────────────
    A track teaches a SKILL and DEMONSTRATES it in a tool, never the reverse.
    Kafka is a LOG, not a queue. If a sentence would be true of a message queue, check it.

    ── WHAT THIS LESSON SHOULD COVER (from curriculum.yaml) ────────────────
     THE EPISODE THAT KEEPS THE TRACK HONEST.
     COVER: what Kafka is bad at, said plainly — it is not a database (no queries, no
     indexes, no random access by anything but offset), not a job queue with per-message
     retries and priorities, and not free to operate. The cases where a table with a
     timestamp column, or a scheduled batch, or a real job queue is the right answer.
     AND THE SIZE QUESTION: most teams running Kafka have volumes a single Postgres table
     would handle. Say so.

    ── HANDS-ON (from curriculum.yaml `hands_on`) ──────────────────────────
      koan:choose
      koan:diagnose
      koan file: tests/src/koans/groovy/datazeus/kafka/series3/_15/WhatKafkaIsBadAtKoans.groovy
   ========================================================================== */}

## The situation this lesson prepares you for

{/* BEAT 1 — the concrete moment at work. Never a definition. */}

TODO

## The principle, then how this tool spells it

{/* BEAT 2 — the general idea first, then the syntax. A track teaches a SKILL and DEMONSTRATES it in a tool, never the reverse. */}

TODO

## See it for yourself

{/* BEAT 3 — NON-NEGOTIABLE. The hands-on above, or the reason there isn't one. */}

TODO

## What goes wrong

{/* BEAT 4 — the wrong number or silent failure this prevents, and how you would notice it. */}

TODO
