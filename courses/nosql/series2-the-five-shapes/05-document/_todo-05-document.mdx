---
title: "Document — an Aggregate Stored and Read as One Thing"
description: "TODO — one plain sentence saying what the reader will be able to do. Same rules as the title: no rhetorical devices, no jargon, about data."
topic: "learn-nosql"
tags: []
level: "intermediate"
module: "series2-the-five-shapes"
order: 5
published: false
---

{/* ==========================================================================
    TODO — NOT A LESSON YET. A brief for whoever writes it, not teaching
    material. Named _todo-* so it stays out of the site build. Drop the prefix
    when it becomes real.

    READ FIRST
      courses/nosql/curriculum.yaml   the track's decisions, in its header
      tests/src/koans/groovy/datazeus/nosql/series2/_05/DocumentKoans.groovy

    ── FOLLOW THE OFFICIAL DOCUMENTATION ───────────────────────────────────
    THE COVERAGE, THE TOPICS AND THE ORDER OF THIS INDIVIDUAL LESSON — not only
    the series order — SHOULD FOLLOW
    THE PRODUCT'S OWN CURRENT DOCUMENTATION for every product named, with the version stated. THIS COURSE HAS NO MANUAL OF ITS OWN — the only documents describing NoSQL as a whole are written by vendors explaining why their product is the answer.
    Open its own section for this topic BEFORE writing, use it as the COVERAGE
    CHECKLIST, and use its VOCABULARY FOR THE NOUNS. Anything left out should be
    left out because an academy rule says so (each engine's own course owns teaching it properly, Learn Data Modeling owns the relational discipline this reverses),
    and NEVER because it was missed.

    ── THE ACADEMY RULE THAT OUTRANKS EVERYTHING ELSE ──────────────────────
    A track teaches a SKILL and DEMONSTRATES it in a tool, never the reverse.
    A comparison page written by one of the databases being compared is a source ABOUT a claim, never the claim. Several famous NoSQL limitations were fixed years ago — check before repeating one.

    ── WHAT THIS LESSON SHOULD COVER (from curriculum.yaml) ────────────────
     COVER: a document is a nested record stored and retrieved as a whole, with the shape
     decided per record rather than per table. THE UNIT OF ATOMICITY IS THE DOCUMENT, which
     is the fact that determines everything else about how you model.
     WHERE IT WINS: an aggregate that is read and written together — an order with its lines,
     a profile with its settings — and a shape that varies per record.
     WHERE IT FAILS: data with many equally valid entry points, where every access pattern
     wants a different aggregate. Naming MongoDB.
     SEAM: Learn SQL, Series 3 · 05 queries JSON inside a relational database and Learn Data
     Modeling, Series 2 · 42 owns deciding to store it. That option is real and often better,
     and this episode should say so rather than leave the reader thinking documents require a
     document database.

    ── HANDS-ON (from curriculum.yaml `hands_on`) ──────────────────────────
      koan:choose
      koan:predict
      koan file: tests/src/koans/groovy/datazeus/nosql/series2/_05/DocumentKoans.groovy
   ========================================================================== */}

## The situation this lesson prepares you for

{/* BEAT 1 — the concrete moment at work. Never a definition. */}

TODO

## The principle, then how this tool spells it

{/* BEAT 2 — the general idea first, then the syntax. A track teaches a SKILL and DEMONSTRATES it in a tool, never the reverse. */}

TODO

## See it for yourself

{/* BEAT 3 — NON-NEGOTIABLE. The hands-on above, or the reason there isn't one. */}

TODO

## What goes wrong

{/* BEAT 4 — the wrong number or silent failure this prevents, and how you would notice it. */}

TODO
