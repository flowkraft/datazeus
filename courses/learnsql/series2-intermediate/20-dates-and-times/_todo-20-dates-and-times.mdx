---
title: "Dates & Times — Getting Ranges and Boundaries Right"
description: "TODO — one plain sentence saying what the reader will be able to do. Same rules as the title."
topic: "learn-sql"
tags: []
level: "intermediate"
module: "series2-intermediate"
order: 20
published: false
---

{/* ==========================================================================
    TODO — NOT A LESSON YET. This file is a brief for whoever writes it, not
    teaching material. It is named _todo-* so it stays out of the site build.
    Drop the _todo- prefix when it becomes a real lesson.

    READ FIRST: courses/learnsql/curriculum.yaml's header — it is the field
    documentation the whole academy points at, and it carries the title, short
    and core conventions this episode has to follow.

    THE HOUSE METHOD, from the three lessons already written:
      - type the query yourself; copy-paste teaches your eyes only
      - PREDICT the answer before running it, out loud, then run it
      - every query runs on the Northwind that ships with DataPallas
      - the koans mirror the lesson beat for beat

    ── WHY THIS EPISODE, SPECIFICALLY ──────────────────────────────────────
    GOAL: answer "last 30 days", "this month", "by quarter" and get the boundaries right.
    SQL: date literals, CURRENT_DATE, INTERVAL, date_trunc, EXTRACT, the half-open range habit
    AND THE ARITHMETIC ITSELF, which the scope above assumes rather than states: date MINUS
    date does not give a number, it gives an INTERVAL (or an integer day count, depending on
    the types) — and that surprise is what makes "days to ship" go wrong. Series 1 · 45
    already CASTs both sides to DATE for exactly this reason, so this episode owes the
    explanation that one borrowed. Cover date + INTERVAL, date - date, and AGE() with the
    caveat that AGE is PostgreSQL's own (Series 4 · 05 carries the portable spelling).
    Named because a competitor audit (2026-09-02) found three of its sixty episodes on this
    one idea — "Adding And Subtracting With Dates", "Extracting Fields From Timestamp",
    "Age Function" — which is a fair signal that beginners get stuck here.
    (>= start AND < next_start) that never miscounts a month.
    Boundaries are the whole episode. BETWEEN on timestamps quietly drops the last day, and that is
    a wrong number nobody questions.

    ── HANDS-ON (from curriculum.yaml) ─────────────────────────────────────
      koan:predict
      koan:diagnose
      koan file: tests/src/koans/groovy/datazeus/learnsql/series2/_20/DatesAndTimesKoans.groovy
   ========================================================================== */}

## The situation this lesson prepares you for

{/* BEAT 1 — the concrete moment at work. The published lessons all open this way;
    keep it. Never "SELECT is used to…". Always "your boss asks…". */}

TODO

## TODO — the SQL

{/* BEAT 2 — the keywords, one at a time, each with a query the reader types.
    Every query runs on Northwind. Predict, then run. */}

TODO

## TODO — see it for yourself

{/* BEAT 3 — point at the koans, and say what they cover. */}

TODO

## What goes wrong

{/* BEAT 4 — the wrong answer this prevents. Every episode in this track earns its
    place by preventing a number that is wrong and looks right. */}

TODO
