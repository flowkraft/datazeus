---
title: "Self-JOINs & Hierarchies — Relating Rows to Other Rows in the Same Table"
description: "TODO — one plain sentence saying what the reader will be able to do. Same rules as the title."
topic: "learn-sql"
tags: []
level: "intermediate"
module: "series2-intermediate"
order: 30
published: false
---

{/* ==========================================================================
    TODO — NOT A LESSON YET. This file is a brief for whoever writes it, not
    teaching material. It is named _todo-* so it stays out of the site build.
    Drop the _todo- prefix when it becomes a real lesson.

    READ FIRST: courses/learnsql/curriculum.yaml's header — it is the field
    documentation the whole academy points at, and it carries the title, short
    and core conventions this episode has to follow.

    THE HOUSE METHOD, from the Series 1 lessons already written:
      - type the query yourself; copy-paste teaches your eyes only
      - PREDICT the answer before running it, out loud, then run it
      - every query runs on the Northwind that ships with DataPallas
      - the koans mirror the lesson beat for beat

    ── WHY THIS EPISODE, SPECIFICALLY ──────────────────────────────────────
    GOAL: relate rows in one table to other rows in the same table.
    SQL: joining a table to itself, aliasing both sides, the manager/employee shape.
    Northwind's "Employees" is thin (3 rows) — say so rather than pretending. It has exactly enough
    for the one idea (checked 2026-09-14): Nancy and Janet report to Andrew, and Andrew's
    "ReportsTo" is NULL. So "each employee beside their manager" loses Andrew under a plain JOIN and
    keeps him under a LEFT JOIN — Series 1 · 40's rule, on a table joined to itself. If the lesson
    needs depth, use a richer example rather than inventing Northwind rows.
    Recursive walking is Series 3 · 10.

    ── WORKPLACE SKILL — what a working person must be able to do after this episode ──────────
    Compare rows with other rows of the same table: employee beside manager (and the LEFT JOIN that keeps
    the one with no manager), pairs of related rows without counting each pair twice or pairing a row with
    itself (a.id < b.id), and a row beside an earlier row the pre-window-function way.

    ── THE SERIES CASE — NONE (.docs/plan-sql-series2-story.md §0, §2, §3) ──────
    This episode's best examples are not the case. At most one line of the case (whose orders; Nancy reports
    to Andrew). PROPOSED, NOT DECIDED (.docs §5.6): make "products bought together" (a self-join on order
    lines) the main example and mirrored pairs the trap — 495 rows with self-matches, 302 mirrored, 151 real
    pairs; Chai + Chang on 9 orders (DuckDB 2026-09-15) — so the headline trap is not Series 1 · 40's LEFT
    JOIN rule a fourth time.

    ── PLACE IN THE SERIES ─────────────────────────────────────────────────
    LEVEL ●● of ●●●●. A different join SHAPE — one table, two roles — and not a third episode about
    rows that drop or multiply, which is why it no longer sits beside Series 2 · 15.
    BUILDS ON: Series 1 · 40. SETS UP: Series 2 · 35, whose window functions replace "a row beside an
    earlier row" done with a self-join, and Series 3 · 10, where a recursive CTE walks the same
    hierarchy. Was 10 until the 2026-09-14 reorder, and 42 until 2026-09-15.

    ── HANDS-ON (from curriculum.yaml) ─────────────────────────────────────
      koan:complete
      koan file: tests/src/koans/groovy/datazeus/learnsql/series2/_30/SelfJoinsAndHierarchiesKoans.groovy
   ========================================================================== */}

## The situation this lesson prepares you for

{/* BEAT 1 — the concrete moment at work. The published lessons all open this way;
    keep it. Never "SELECT is used to…". Always "your boss asks…". */}

TODO

## TODO — the SQL

{/* BEAT 2 — the keywords, one at a time, each with a query the reader types.
    Every query runs on Northwind. Predict, then run. */}

TODO

## TODO — see it for yourself

{/* BEAT 3 — point at the koans, and say what they cover. */}

TODO

## What goes wrong

{/* BEAT 4 — the wrong answer this prevents. Every episode in this track earns its
    place by preventing a number that is wrong and looks right.
    The one to write: the org chart that lost its boss. A plain self-JOIN drops Andrew,
    because his "ReportsTo" is empty — and the chart of three people now shows two. */}

TODO
