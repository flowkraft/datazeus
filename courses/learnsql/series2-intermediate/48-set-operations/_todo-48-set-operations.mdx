---
title: "UNION, INTERSECT, EXCEPT — Stacking and Reconciling Result Sets"
description: "TODO — one plain sentence saying what the reader will be able to do. Same rules as the title."
topic: "learn-sql"
tags: []
level: "intermediate"
module: "series2-intermediate"
order: 48
published: false
---

{/* ==========================================================================
    TODO — NOT A LESSON YET. This file is a brief for whoever writes it, not
    teaching material. It is named _todo-* so it stays out of the site build.
    Drop the _todo- prefix when it becomes a real lesson.

    READ FIRST: courses/learnsql/curriculum.yaml's header — it is the field
    documentation the whole academy points at, and it carries the title, short
    and core conventions this episode has to follow.

    THE HOUSE METHOD, from the Series 1 lessons already written:
      - type the query yourself; copy-paste teaches your eyes only
      - PREDICT the answer before running it, out loud, then run it
      - every query runs on the Northwind that ships with DataPallas
      - the koans mirror the lesson beat for beat

    ── WHY THIS EPISODE, SPECIFICALLY ──────────────────────────────────────
    GOAL: stack two result sets, or subtract one from the other.
    SQL: UNION, UNION ALL, INTERSECT, EXCEPT, and the column-count/type rules.
    UNION ALL versus UNION is the practical half: UNION deduplicates and costs a sort, and people
    reach for it by habit when they wanted UNION ALL.

    ── WORKPLACE SKILL — what a working person must be able to do after this episode ──────────
    Stack result sets from two sources (UNION ALL), remove duplicates only on purpose (UNION, and the sort
    it costs), reconcile two lists (EXCEPT both ways, INTERSECT), the column count / type / name rules, and
    ORDER BY on the combined result.

    ── THE SERIES CASE — MAIN EXAMPLE — reconciliation (story map: .docs/plan-academy-course-stories-artefacts.md §3.2.1) ──────
    Reconciling two lists is what INTERSECT and EXCEPT are for at work, and the case's rows are the best rows to
    teach it on. Speedy Express sends its delivery confirmations ("CourierConfirmations"):
      our un-dated Speedy Express orders placed 2024-03-04..08-30 INTERSECT the confirmed order IDs → delivered,
      invoice them; EXCEPT → never confirmed, claim them. LOCKED (gate 2, 2026-09-17, both engines):
      177 un-dated · 164 confirmed (€242,383.04) · 13 not (€14,511.63); 173 confirmation rows, 172 distinct orders,
      8 for orders that already have a ship date.
    The noise is part of the lesson: a few confirmations are for orders that already have a ship date (EXCEPT the
    other way round finds them), and one confirmation was sent twice (UNION vs UNION ALL when counting them).
    CORRECTED 2026-09-15: Series 1 · 40 has no UNION. The real callback: it says FULL OUTER JOIN earns its place
    in reconciling two lists; EXCEPT/INTERSECT are the set-based way to do it.
    WHAT GOES WRONG: UNION instead of UNION ALL when stacking two real sources — the duplicate confirmation
    collapses and the count comes back short with no error.

    DATA (decided 2026-09-17): this episode runs on NORTHWIND COMPANY S (schema northwind_co_s, installed by the
    academy-northwind-co-install.groovy Seed Data script) — lesson, case and koans alike. Every figure below that
    came from the frozen Northwind is to be RE-FOUND on it: the lesson beat stays, the numbers move. First
    measurements are DuckDB only (stories sheet §3.2.2); every figure is re-asserted on PostgreSQL in the spec.

    ── REMINDER: CURRICULUM FIRST, THE STORY FROM TIME TO TIME (owner, 2026-09-17) ─────────
    Series 2 is first an intermediate SQL course that covers its curriculum well and progresses steadily; the
    story (the Speedy Express recording outage) goes along from time to time — in 8 of 16 episodes, never whole
    episodes. Story map: .docs/plan-academy-course-stories-artefacts.md §3.2.1. THIS EPISODE: MAIN EXAMPLE — reconciliation.
    A SEGMENT IS ONE QUERY of the shape the lesson just taught, and ONE finding, after a full lesson — using
    nothing the learner has not met yet. If it needs more, it belongs to a later episode or to the project.
    WHY: a learner came for the SQL; the story is there to make it stick, and a story that crowds a lesson or
    repeats itself does the opposite. Checking in the brief is cheaper than re-cutting a published video.

    ── PLACE IN THE SERIES ─────────────────────────────────────────────────
    LEVEL ●● of ●●●●. THE CALLBACK: Series 1 · 40 says FULL OUTER JOIN earns its place in reconciling two
    lists that should agree; EXCEPT and INTERSECT are the set-based way (see THE SERIES CASE above — the old
    "S1·40 wrote a FULL OUTER JOIN as a UNION" callback was false). A good koan: prove an EXCEPT and a
    NOT EXISTS (Series 2 · 25) return the same rows, on a different question from the lesson's.
    BUILDS ON: Series 2 · 25, Series 1 · 40. SETS UP: Series 2 · 50, which replaces the UNION ALL report. Was 35 until 2026-09-15.

    ── HANDS-ON (from curriculum.yaml) ─────────────────────────────────────
      koan:predict
      koan:choose
      koan file: tests/src/koans/groovy/datazeus/learnsql/series2/_48/SetOperationsKoans.groovy
   ========================================================================== */}

## The situation this lesson prepares you for

{/* BEAT 1 — the concrete moment at work. The published lessons all open this way;
    keep it. Never "SELECT is used to…". Always "your boss asks…". */}

TODO

## TODO — the SQL

{/* BEAT 2 — the keywords, one at a time, each with a query the reader types.
    Every query runs on Northwind. Predict, then run. */}

TODO

## TODO — see it for yourself

{/* BEAT 3 — point at the koans, and say what they cover. */}

TODO

## What goes wrong

{/* BEAT 4 — the wrong answer this prevents. Every episode in this track earns its
    place by preventing a number that is wrong and looks right. */}

TODO
