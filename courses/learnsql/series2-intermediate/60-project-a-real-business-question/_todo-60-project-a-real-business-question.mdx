---
title: "Subqueries + CTEs + Window Functions — Solving a Real Business Case, End to End"
description: "TODO — one plain sentence saying what the reader will be able to do. Same rules as the title."
topic: "learn-sql"
tags: ["project"]
level: "intermediate"
module: "series2-intermediate"
order: 60
published: false
---

{/* ==========================================================================
    TODO — NOT A LESSON YET. This file is a brief for whoever writes it, not
    teaching material. It is named _todo-* so it stays out of the site build.
    Drop the _todo- prefix when it becomes a real lesson.

    READ FIRST: courses/learnsql/curriculum.yaml's header — it is the field
    documentation the whole academy points at, and it carries the title, short
    and core conventions this episode has to follow.

    THE HOUSE METHOD, from the Series 1 lessons already written:
      - type the query yourself; copy-paste teaches your eyes only
      - PREDICT the answer before running it, out loud, then run it
      - every query runs on the Northwind that ships with DataPallas
      - the koans mirror the lesson beat for beat

    WHY THIS FILE EXISTS: it was the ONE episode in Learn SQL with neither a
    lesson brief nor a koan brief — 34 of 35 had both. The gap was invisible
    until the track declared `hands_on` on 2026-08-24 and something finally
    compared the two lists. That it was the SERIES PROJECT is the worst place
    for it: the episode where everything gets applied is the one a reader most
    needs written well.

    ── WHY THIS EPISODE, SPECIFICALLY ──────────────────────────────────────
    GOAL: take one question a person would actually be asked at work and answer it
    correctly, end to end — not a drill, and not a question that hands you its own
    answer.
    SQL: NOTHING NEW, and say so out loud in the article. Subqueries (00) and EXISTS (25), multi-table joins and their grain
    (15), a CTE to name the steps (20), CASE for the buckets (05),
    a date range with the right boundaries (10), a window function for the rank or the
    running total (35/45). A project introduces no syntax; that is what makes it one.

    ── A QUESTION THAT HAS WRONG ANSWERS THAT LOOK RIGHT — CHOSEN 2026-09-15 ────
    Without that property, "end to end" just means "longer", and a longer drill is still a
    drill. The question is the series case (THE SERIES CASE below, decided 2026-09-17): 2024's
    ordered sales grew while invoiced sales fell — where did the money go, and were those orders
    delivered? The
    article must NAME the numbers a careful person gets wrong on the way, and the koans must
    be able to tell the right answer from them.
    (The two earlier candidates are dropped: revenue per customer per quarter answered nothing
    the series asks, and "bought in Q1 and not since" is not churn on this data.)

    ── END ON THE HABIT, NOT THE QUERY ─────────────────────────────────────
    The last beat is the check, not the answer: after the join, did the number of
    distinct customers change? Three lines, transferable to every tool, and it is what
    would have caught the wrong answer above. Java & Groovy Series 1 · 40 and Python
    Series 1 · 35 close on the same check on purpose — a reader who does two of these
    tracks should recognise it and know why it followed them.

    ── WORKPLACE SKILL — what a working person must be able to do after this episode ──────────
    Take a real question end to end: steps that each run on their own, the row-count and total checks after
    every join, a query someone can re-run, and a written recommendation that says what the numbers do and do
    not prove. Nothing new in the SQL — say so.

    ── THE SERIES CASE — THE WHOLE CASE (story map: .docs/plan-academy-course-stories-artefacts.md §3.2.1) ──────
    THE QUESTION: in Northwind Company, 2024's ordered sales grew while invoiced sales fell. Where did the money
    go, were those orders delivered, and what do we do? Nothing new in the SQL — say so. The report for the
    manager, each step runnable on its own, with the row-count and total checks:
      the gap — ordered vs invoiced, 2024 (Series 2 · 00);
      the courier — "marked shipped, no ship date" is Speedy Express, not the others (· 05);
      the dates — a steady trickle from the week of 2024-03-04 to the end of August 2024, 0–1 a week outside (· 10);
      the money — shipped orders with no invoice (· 15);
      the evidence, said as soft — customers ordered again and were served (· 25);
      the reconciliation — confirmed vs never confirmed (· 48); the fix, in a transaction (· 55);
      WHAT ONLY THE PROJECT FINDS — orders still 'Open' long after they should have shipped: cancelled by the
      customer and never updated, phantom revenue in the ordered report (LOCKED: 43 orders, €59,611.41); and the claim
      deadline: 12 months from the order, the earliest expiring 2025-03-08.
    THE RECOMMENDATION, not a verdict: invoice the confirmed orders now; file claims for the unconfirmed before
    the windows close; restate the phantom sales; make every report say whether it counts ordered or delivered;
    say plainly what the data cannot prove. The stake (figures sheet .docs/plan-academy-figures-northwind-co-s.md; first
    measurement → LOCKED at gate 2: 177 outage orders, €256,895 never invoiced; ≈ €247k protected).
    ORDERED SALES WERE NOT WRONG (owner, 2026-09-16): they measure demand and answered that question right.
    THE WRONG ANSWERS THAT LOOKED RIGHT, named in the article: "sales are fine, orders are up"; "customers are
    paying late" (finance's first explanation); "it's seasonal"; "Speedy Express lost 177 orders" (the
    confirmations say otherwise); "switch courier".
    KOANS: a different question on different tables, as Series 1 · 50's koans did, ending on the habit check (the
    key count survives every join). Measure first.
    RULES: "in Northwind Company"; no person blamed; a recommendation, not a verdict.

    DATA (decided 2026-09-17): this episode runs on NORTHWIND COMPANY S (schema northwind_co_s, installed by the
    academy-northwind-co-install.groovy Seed Data script) — lesson, case and koans alike. Every figure below that
    came from the frozen Northwind is to be RE-FOUND on it: the lesson beat stays, the numbers move. First
    measurements are DuckDB only (stories sheet §3.2.2); every figure is re-asserted on PostgreSQL in the spec.

    ── REMINDER: CURRICULUM FIRST, THE STORY FROM TIME TO TIME (owner, 2026-09-17) ─────────
    Series 2 is first an intermediate SQL course that covers its curriculum well and progresses steadily; the
    story (the Speedy Express recording outage) goes along from time to time — in 8 of 16 episodes, never whole
    episodes. Story map: .docs/plan-academy-course-stories-artefacts.md §3.2.1. THIS EPISODE: MAIN EXAMPLE — the whole case.
    A SEGMENT IS ONE QUERY of the shape the lesson just taught, and ONE finding, after a full lesson — using
    nothing the learner has not met yet. If it needs more, it belongs to a later episode or to the project.
    WHY: a learner came for the SQL; the story is there to make it stick, and a story that crowds a lesson or
    repeats itself does the opposite. Checking in the brief is cheaper than re-cutting a published video.

    ── REMINDER: THE EVIDENCE IS SOFT — SAY SO LOUDLY (owner review, 2026-09-16) ─────────────────
    Customers ordering again and being served normally SUGGESTS the goods arrived; it does NOT PROVE it. Say it
    out loud, in the video (a spoken line, not a footnote) and in the article: it is also what a customer on a
    standing schedule would do whatever happened to one order; SQL narrows it down, people (and the courier's
    confirmations, Series 2 · 48) confirm it. Suggested line for Mnemosyne, to adapt: "That's what delivered
    orders would look like. It isn't proof — it's the reason to ask the courier before anyone calls a customer."
    WHY: it is true; the course rules out verdicts; separating what data shows from what it proves is the
    workplace skill; and it is what makes "ask the courier first" the sensible recommendation.

    ── PLACE IN THE SERIES ─────────────────────────────────────────────────
    The last episode of the series; it may use anything above it.
    DATA: every figure of the case is in .docs/plan-academy-course-stories-artefacts.md Appendix A (DuckDB, 2026-09-15) —
    re-assert each on PostgreSQL in the spec before the article uses it.

    ── HANDS-ON (from curriculum.yaml) ─────────────────────────────────────
      koan:author
      cloudbeaver
      koan file: tests/src/koans/groovy/datazeus/learnsql/series2/_60/ProjectARealBusinessQuestionKoans.groovy
   ========================================================================== */}

## The situation this lesson prepares you for

{/* BEAT 1 — the concrete moment at work. The published lessons all open this way;
    keep it. Never "SELECT is used to…". Always "your boss asks…". */}

TODO

## TODO — the SQL

{/* BEAT 2 — build the query in steps, each one runnable on its own, so a reader who
    stalls knows WHICH step broke. Every query runs on Northwind. Predict, then run. */}

TODO

## TODO — see it for yourself

{/* BEAT 3 — NON-NEGOTIABLE. The koans above. This episode's rung is `koan:author`:
    no scaffolding, one blank for the whole query, graded on the row set. */}

TODO

## What goes wrong

{/* BEAT 4 — the wrong number this prevents, and how you would notice it. Name it
    explicitly: this is the beat the whole episode was designed around. */}

TODO
