---
title: "Data Types — What a Column Can Hold, and Why Results Surprise You"
description: "TODO — one plain sentence saying what the reader will be able to do. Same rules as the title."
topic: "learn-sql"
tags: []
level: "beginner"
module: "series1-fundamentals"
order: 7
published: false
---

{/* ==========================================================================
    TODO — NOT A LESSON YET. This file is a brief for whoever writes it, not
    teaching material. It is named _todo-* so it stays out of the site build.
    Drop the _todo- prefix when it becomes a real lesson.

    READ FIRST: courses/learnsql/curriculum.yaml's header — it is the field
    documentation the whole academy points at, and it carries the title, short
    and core conventions this episode has to follow.

    THE HOUSE METHOD, from the three lessons already written:
      - type the query yourself; copy-paste teaches your eyes only
      - PREDICT the answer before running it, out loud, then run it
      - every query runs on the Northwind that ships with DataPallas
      - the koans mirror the lesson beat for beat

    ── WHY THIS EPISODE, SPECIFICALLY ──────────────────────────────────────
    GOAL: a reader who can say what kind of thing a column holds, and who is no longer
    surprised by a result that "looks wrong".
    SQL: no new clause. The type families (whole numbers, decimals, text, dates, true/false),
    why text needs 'single quotes' and numbers do not, and information_schema.columns as
    "ask the database what it is holding".

    IT SITS BEFORE WHERE ON PURPOSE. Episode 10 currently improvises half of this — it has to
    explain "'single quotes' for text, double quotes for names, numbers bare" before it can
    teach a single comparison. Teach it here and 10 gets to be about WHERE.

    WHERE and ORDER BY are still untaught at this point, so show the catalog query WITHOUT
    them. They only ever made its output tidier.

    ── INHERITED FROM EPISODE 05, 2026-08-26 ───────────────────────────────
    This episode exists because 05 tried to carry data types and could not. What was cut:

      "why does a price of 18 come back as 18.0000?" — a good hook that DOES NOT FIRE in
      CloudBeaver, the client this course tells the reader to open: it trims trailing zeros
      and shows 18. psql shows 18.0000. Both are right — the column is numeric(19,4) on
      PostgreSQL and DECIMAL(10,4) on DuckDB. Do NOT rebuild the beat around a value the
      reader's own screen will not show. Either use a client-independent example, or make the
      disagreement itself the lesson — but that version belongs in Series 4 · 00.

      information_schema.columns — kept, and it is the heart of this episode.

    ROUND went to Series 1 · 20 (it is an expression, and the engine's auto-generated column
    name is the argument for aliases). The DuckDB-vs-PostgreSQL type vocabulary went to
    Series 4 · 00.

    WATCH OUT: the shipped DuckDB and the shipped PostgreSQL DO NOT have identical schemas
    (as of 2026-08-26: Categories 3 vs 4 columns, Employees 18 vs 20, and a different column
    order). Verify every claim against BOTH engines before writing a number down — the gate
    only does that when PGHOST points at a real DataPallas Northwind.

    ── HANDS-ON (from curriculum.yaml) ─────────────────────────────────────
      koan:predict
      koan:complete
      koan file: tests/src/koans/groovy/datazeus/learnsql/series1/_07/DataTypesKoans.groovy
   ========================================================================== */}

## The situation this lesson prepares you for

{/* BEAT 1 — the concrete moment at work. The published lessons all open this way;
    keep it. Never "SELECT is used to…". Always "your boss asks…". */}

TODO

## TODO — the SQL

{/* BEAT 2 — the keywords, one at a time, each with a query the reader types.
    Every query runs on Northwind. Predict, then run. */}

TODO

## TODO — see it for yourself

{/* BEAT 3 — point at the koans, and say what they cover. */}

TODO

## What goes wrong

{/* BEAT 4 — the wrong answer this prevents. Every episode in this track earns its
    place by preventing a number that is wrong and looks right. */}

TODO
