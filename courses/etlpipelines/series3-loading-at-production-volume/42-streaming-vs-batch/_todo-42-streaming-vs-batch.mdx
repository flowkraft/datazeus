---
title: "Streaming vs Batch — Is Your Data Bounded, and Do You Need a Stream?"
description: "TODO — one plain sentence saying what the reader will be able to do. Same rules as the title: no rhetorical devices, no jargon, about data."
topic: "learn-etl-pipelines"
tags: []
level: "advanced"
module: "series3-loading-at-production-volume"
order: 42
published: false
---

{/* ==========================================================================
    TODO — NOT A LESSON YET. A brief for whoever writes it, not teaching
    material. Named _todo-* so it stays out of the site build. Drop the prefix
    when it becomes real.

    READ FIRST
      courses/etlpipelines/curriculum.yaml   the track's decisions, in its header
      tests/src/koans/groovy/datazeus/etlpipelines/series3/_42/StreamingVsBatchKoans.groovy

    ── THE RULE THAT OUTRANKS EVERYTHING ELSE HERE ─────────────────────────
    This track teaches a SKILL and DEMONSTRATES it in a tool. Never the reverse.
    Whatever the reader learns must still be true away from our tools.

    ── WHERE THIS SITS ─────────────────────────────────────────────────────
    ETL & Data Pipelines · Production Data Flow (advanced)
      before:  Change Data Capture — Stream the Deltas Instead of Reloading
      THIS:    Streaming vs Batch — the DECISION
      after:   Project — Orchestrate the Full Northwind Pipeline End to End
    CORE.

    ── WHY THIS EPISODE, SPECIFICALLY ──────────────────────────────────────
    GOAL: decide whether a stated requirement actually needs a stream.
    DECISION: it fills a slot that was already empty, and 15 directly above says "STREAM the
    deltas" without ever saying what a stream is.

    SPLIT FROM 22 ON 2026-09-02. This episode used to carry the mechanics too and was at risk
    of becoming the Kafka and Flink courses. Adding a BATCH episode and a STREAMING episode in
    front was considered and rejected: Series 1 and 2 are twenty-three episodes of batch, so a
    reader here has BUILT two dozen batch pipelines and explaining batch now inverts the
    course; and a streaming episode would be either this one's first half or Flink's course.
    The split is by CONCEPT DEPTH — decision here, mechanics at 22.

    ── SCOPE, AND THE LINE NOT TO CROSS ────────────────────────────────────
      1. BOUNDED vs UNBOUNDED — a batch has an end, a stream does not. Everything follows,
         including why "run it more often" is a different thing from streaming.
      2. WHEN IS A BATCH COMPLETE — the one genuinely batch-shaped question this course never
         asks. How do you know all of Monday's data arrived before Monday's load runs? Arrival
         signals, a sentinel file, wait-then-run. ONE PARAGRAPH: the machinery is Airflow's
         sensors and the late-data half is 2 · 20.
      3. THE DECISION ITSELF — what the koan tests.

    EVENT TIME, WINDOWS AND WATERMARKS ARE NAMED HERE AND DEFINED AT 22. Name them and move
    on. If this beat starts explaining them, the split has undone itself.

    THE THREE TOOLS, ONE PARAGRAPH EACH, named for what they ARE and not taught:
      Kafka  a LOG — transport. Not a queue, and not a processor.
      Spark  a distributed BATCH engine, with streaming added as micro-batches.
      Flink  an event-time STREAM processor.
    THE LINE: partitions, consumer groups, checkpoints and shuffle belong to those courses.

    ── THE THESIS — ALREADY THE HOUSE LINE, DO NOT SOFTEN IT ───────────────
    dbt says it outright ("it is not Spark. The warehouse does 100% of the work"); Data
    Warehousing pushes compute into DuckDB and ClickHouse; this track is SQL-first. So:
        STREAMING IS A MODELLING DECISION BEFORE IT IS A TOOLING DECISION, and most
        "we need streaming" turns out to be "we need hourly instead of daily" — which CDC at
        15 already gives you, with none of these three.

    ── HANDS-ON (from curriculum.yaml) ─────────────────────────────────────
      koan:choose
      koan file: tests/src/koans/groovy/datazeus/etlpipelines/series3/_42/StreamingVsBatchKoans.groovy
   ========================================================================== */}

## The situation this lesson prepares you for

{/* BEAT 1 — the concrete moment at work. Never a definition.

    Somebody asks for "real-time" dashboards. Find out what they actually mean — how stale is
    too stale, and what decision changes if the number is an hour old. That conversation is
    the episode's real subject. */}

TODO

## TODO — the principle

{/* BEAT 2 — the general idea, true anywhere. Bounded against unbounded, then the
    completeness question, then the three tools named. No windows, no watermarks. */}

TODO

## TODO — see it for yourself

{/* BEAT 3 — NON-NEGOTIABLE. The koan above, or the reason there isn't one.

    choose: several requirements, and the reader picks batch, a smaller batch window, CDC, or
    a genuine stream. Most should resolve to something other than a stream — and one
    genuinely should not, or the koan is rigged. */}

TODO

## What goes wrong

{/* BEAT 4 — the wrong number this prevents, and how you would notice it.

    THE FAILURE HERE IS ORGANISATIONAL, and say so plainly rather than manufacturing a wrong
    total: a team adopts Kafka and Flink to satisfy a requirement a fifteen-minute batch would
    have met, and now maintains a streaming platform to produce a daily report. The wrong
    NUMBERS live at 22. */}

TODO
