---
title: "Backing Up a Database That Is One File — EXPORT DATABASE and the Version Question"
description: "TODO — one plain sentence saying what the reader will be able to do. Same rules as the title: no rhetorical devices, no jargon, about data."
topic: "learn-duckdb"
tags: []
level: "intermediate"
module: "series1-duckdb-essentials"
order: 22
published: false
---

{/* ==========================================================================
    TODO — NOT A LESSON YET. A brief for whoever writes it, not teaching
    material. Named _todo-* so it stays out of the site build. Drop the prefix
    when it becomes real.

    READ FIRST
      courses/duckdb/curriculum.yaml   the track's decisions, in its header
      tests/src/koans/groovy/datazeus/duckdb/series1/_22/BackingUpOneFileKoans.groovy

    ── FOLLOW THE OFFICIAL DOCUMENTATION ───────────────────────────────────
    THE COVERAGE, THE TOPICS AND THE ORDER OF THIS INDIVIDUAL LESSON — not only
    the series order — SHOULD FOLLOW
    the official DuckDB documentation (duckdb.org/docs).
    Open its own section for this topic BEFORE writing, use it as the COVERAGE
    CHECKLIST, and use its VOCABULARY FOR THE NOUNS. Anything left out should be
    left out because an academy rule says so (Learn SQL owns the SQL, Data Warehousing owns columnar storage),
    and NEVER because it was missed.

    ── THE ACADEMY RULE THAT OUTRANKS EVERYTHING ELSE ──────────────────────
    A track teaches a SKILL and DEMONSTRATES it in a tool, never the reverse.
    This is a DELTA track: every sentence should be one that would be FALSE on another engine.

    ── WHAT THIS LESSON SHOULD COVER (from curriculum.yaml) ────────────────
     ADDED 2026-09-02. This course had NO episode and no exclude on backup — the only engine
     in the catalogue that was simply silent, which an audit caught.
     THE ANSWER IS SHORT AND WORTH SAYING ANYWAY: the database is ONE FILE, so a backup is a
     copy of that file taken while nothing is writing. There is no server to quiesce and no
     log to ship.
     COVER: EXPORT DATABASE and IMPORT DATABASE as the portable form — Parquet or CSV plus
     schema, which survives a DuckDB version bump where a raw file may not, and that
     durability point is the reason the episode exists rather than a footnote. ATTACH plus
     COPY FROM DATABASE for copying between databases. What is NOT a backup: an in-memory
     database, and a file on the same disk.
     THE TRAP: copying the file while a connection is writing. Contrast SQLite, which
     provides VACUUM INTO and a backup API precisely because people do this.

    ── HANDS-ON (from curriculum.yaml `hands_on`) ──────────────────────────
      koan:process:tool
      koan:diagnose
      koan file: tests/src/koans/groovy/datazeus/duckdb/series1/_22/BackingUpOneFileKoans.groovy
   ========================================================================== */}

## The situation this lesson prepares you for

{/* BEAT 1 — the concrete moment at work. Never a definition. */}

TODO

## The principle, then how this tool spells it

{/* BEAT 2 — the general idea first, then the syntax. A track teaches a SKILL and DEMONSTRATES it in a tool, never the reverse. */}

TODO

## See it for yourself

{/* BEAT 3 — NON-NEGOTIABLE. The hands-on above, or the reason there isn't one. */}

TODO

## What goes wrong

{/* BEAT 4 — the wrong number or silent failure this prevents, and how you would notice it. */}

TODO
