---
title: "Making the Hot Node Happen, Then Fixing It — Hash-Sharded Indexes and UUIDs"
description: "TODO — one plain sentence saying what the reader will be able to do. Same rules as the title: no rhetorical devices, no jargon, about data."
topic: "learn-cockroachdb"
tags: []
level: "advanced"
module: "series2-schema-and-transactions"
order: 0
published: false
---

{/* ==========================================================================
    TODO — NOT A LESSON YET. A brief for whoever writes it, not teaching
    material. Named _todo-* so it stays out of the site build. Drop the prefix
    when it becomes real.

    READ FIRST
      courses/cockroachdb/curriculum.yaml   the track's decisions, in its header
      tests/src/koans/groovy/datazeus/cockroachdb/series2/_00/KeysThatSpreadKoans.groovy

    ── FOLLOW THE OFFICIAL DOCUMENTATION ───────────────────────────────────
    THE COVERAGE, THE TOPICS AND THE ORDER OF THIS INDIVIDUAL LESSON — not only
    the series order — SHOULD FOLLOW
    the official CockroachDB documentation — its Develop, Deploy and Migrate sections, plus the POSTGRESQL COMPATIBILITY page and the SQL PERFORMANCE BEST PRACTICES page, which are the two most useful documents in the set for this reader.
    Open its own section for this topic BEFORE writing, use it as the COVERAGE
    CHECKLIST, and use its VOCABULARY FOR THE NOUNS. Anything left out should be
    left out because an academy rule says so (Distributed SQL owns the decisions, Learn SQL owns the SQL, Data Ops owns the machine, Data Warehousing owns analytics),
    and NEVER because it was missed.

    ── THE ACADEMY RULE THAT OUTRANKS EVERYTHING ELSE ──────────────────────
    A track teaches a SKILL and DEMONSTRATES it in a tool, never the reverse.
    "PostgreSQL compatible" is a wire protocol and a dialect, not a promise of behaviour. State the VERSION AND EDITION; both have moved.

    ── WHAT THIS LESSON SHOULD COVER (from curriculum.yaml) ────────────────
     COVER: the concepts track's sequential-key problem, created deliberately and then fixed.
     Insert with a sequential key, watch one node take the whole load in the console, and see
     the difference when the key spreads.
     THE OPTIONS IN THIS PRODUCT, with their costs: UUID keys; hash-sharded indexes, which
     keep an ordered column usable while spreading the writes; and compound keys with a varied
     leading column. Show the DDL for each.
     AND THE COST TO DEMONSTRATE RATHER THAN ASSERT: a spread key scatters the range scan you
     wanted. Run both and let the reader see both numbers.

    ── HANDS-ON (from curriculum.yaml `hands_on`) ──────────────────────────
      koan:diagnose
      koan:complete
      koan file: tests/src/koans/groovy/datazeus/cockroachdb/series2/_00/KeysThatSpreadKoans.groovy
   ========================================================================== */}

## The situation this lesson prepares you for

{/* BEAT 1 — the concrete moment at work. Never a definition. */}

TODO

## The principle, then how this tool spells it

{/* BEAT 2 — the general idea first, then the syntax. A track teaches a SKILL and DEMONSTRATES it in a tool, never the reverse. */}

TODO

## See it for yourself

{/* BEAT 3 — NON-NEGOTIABLE. The hands-on above, or the reason there isn't one. */}

TODO

## What goes wrong

{/* BEAT 4 — the wrong number or silent failure this prevents, and how you would notice it. */}

TODO
