---
title: "MySQL vs PostgreSQL — Strictness, Index Kinds, and What Actually Differs"
description: "TODO — one plain sentence saying what the reader will be able to do. Same rules as the title: no rhetorical devices, no jargon, about data."
topic: "learn-database-comparisons"
tags: []
level: "intermediate"
module: "series1-row-store-vs-row-store"
order: 0
published: false
---

{/* ==========================================================================
    TODO — NOT A LESSON YET. A brief for whoever writes it, not teaching
    material. Named _todo-* so it stays out of the site build. Drop the prefix
    when it becomes real.

    READ FIRST
      courses/dbcomparisons/curriculum.yaml   the track's decisions, in its header
      tests/src/koans/groovy/datazeus/dbcomparisons/series1/_00/MysqlVsPostgresqlKoans.groovy

    ── FOLLOW THE OFFICIAL DOCUMENTATION ───────────────────────────────────
    THE COVERAGE, THE TOPICS AND THE ORDER OF THIS INDIVIDUAL LESSON — not only
    the series order — SHOULD FOLLOW
    BOTH engines' official documentation — this course has no manual of its own.
    Open its own section for this topic BEFORE writing, use it as the COVERAGE
    CHECKLIST, and use its VOCABULARY FOR THE NOUNS. Anything left out should be
    left out because an academy rule says so (each engine's own course owns teaching it properly),
    and NEVER because it was missed.

    ── THE ACADEMY RULE THAT OUTRANKS EVERYTHING ELSE ──────────────────────
    A track teaches a SKILL and DEMONSTRATES it in a tool, never the reverse.
    EVERY CLAIM MUST BE CHECKED AGAINST BOTH ENGINES' DOCS ON THE DAY IT IS WRITTEN, with versions stated. Vendor comparison pages are sources ABOUT a claim, never the claim.

    ── WHAT THIS LESSON SHOULD COVER (from curriculum.yaml) ────────────────
     THE FLAGSHIP COMPARISON, and the one that sets the tone for the whole course.
     AGREES ON: ACID, MVCC, joins, window functions, CTEs, JSON columns, replication,
     foreign keys, and being fast enough for almost anything. Say all of that first.
     GENUINELY DIFFERS: strictness by default (sql_mode against a server that simply
     refuses); the clustered primary key, which changes how you choose a PK and what a
     secondary index costs; index kinds — Postgres has five, MySQL has B-tree and FULLTEXT;
     extensions as a whole culture against a single-vendor feature set; JSON depth;
     DDL transactionality, where Postgres can roll a migration back and MySQL cannot.
     THE QUESTION THAT DECIDES IT: is the workload doing anything Postgres's index types or
     extensions exist for — and if not, who already runs one of them?

    ── HANDS-ON (from curriculum.yaml `hands_on`) ──────────────────────────
      koan:choose
      koan:predict
      koan file: tests/src/koans/groovy/datazeus/dbcomparisons/series1/_00/MysqlVsPostgresqlKoans.groovy
   ========================================================================== */}

## The decision this lesson helps you make

{/* BEAT 1 — someone has to choose, and there is a deadline. */}

TODO

## What they agree on

{/* BEAT 2 — FIRST, and non-negotiable. Most of the surface is identical; opening on differences makes two engines look further apart than they are. */}

TODO

## What genuinely differs

{/* BEAT 3 — only what survives checking both manuals. Versions stated. */}

TODO

## The question that actually decides it

{/* BEAT 4 — allowed to be 'it does not matter, use the one your team already runs'. */}

TODO
