---
title: "Installing a Host — and Why dom0 Is Not Somewhere You Put Things"
description: "TODO — one plain sentence saying what the reader will be able to do. Same rules as the title: no rhetorical devices, no jargon, about data."
topic: "learn-virtualization"
tags: []
level: "intermediate"
module: "series2-xcp-ng"
order: 2
published: false
---

{/* ==========================================================================
    TODO — NOT A LESSON YET. A brief for whoever writes it, not teaching
    material. Named _todo-* so it stays out of the site build. Drop the prefix
    when it becomes real.

    READ FIRST
      courses/virtualization/curriculum.yaml   the track's decisions, in its header
      tests/src/koans/groovy/datazeus/virtualization/series2/_02/InstallingAndDom0Koans.groovy

    ── FOLLOW THE OFFICIAL DOCUMENTATION ───────────────────────────────────
    THE COVERAGE, THE TOPICS AND THE ORDER OF THIS INDIVIDUAL LESSON — not only
    the series order — SHOULD FOLLOW
    the project that owns the series — the XCP-ng docs and the Xen Orchestra docs for Series 2, the Proxmox VE Administration Guide for Series 3, the TrueNAS documentation for Series 4. SERIES 1 HAS NO PRODUCT MANUAL: it is the kernel, libvirt and the Linux Containers project.
    Open its own section for this topic BEFORE writing, use it as the COVERAGE
    CHECKLIST, and use its VOCABULARY FOR THE NOUNS. Anything left out should be
    left out because an academy rule says so (Data Ops owns renting a machine and using Docker, Storage for Data owns the disks and ZFS itself, Kubernetes for Data Workloads owns what schedules pods onto these machines),
    and NEVER because it was missed.

    ── THE ACADEMY RULE THAT OUTRANKS EVERYTHING ELSE ──────────────────────
    A track teaches a SKILL and DEMONSTRATES it in a tool, never the reverse.
    EVERY EPISODE OPENS ON A MOMENT SOMEBODY HAS ACTUALLY HAD AT WORK — that is what this course has that the manual does not. A SNAPSHOT IS NOT A BACKUP, and HA OF A VM IS NOT HA OF A DATABASE. THE RUNBOOK IS THE CLI, never a screenshot. ⚠ VERSION-DEPENDENT CLAIMS MUST BE CHECKED, NEVER REMEMBERED: this subject has moved recently.

    ── WHAT THIS LESSON SHOULD COVER (from curriculum.yaml) ────────────────
     COVER: the install, as a runbook, and the concept that install forces you to meet.
     THE INSTALLER: it takes the whole disk, it is not Debian, and the local storage
     repository is created for you on what is left. Say what it asks and what to answer.
     THEN DOM0, AND THE RULE THAT IS THE MOST USEFUL SENTENCE IN THIS SERIES: DOM0 IS NOT
     YOUR SERVER. It is a control domain with a deliberately small memory allocation and a
     small root filesystem, and it gets REPLACED on major upgrades. So:
       DO NOT install your monitoring stack, your scripts, your Python, or your data tools in
       it. Do not fill /var. Every one of those is lost or in the way at upgrade time.
       ANYTHING YOU WANT TO RUN GOES IN A VM — including, and especially, the management
       plane in Series 2 · 05.
     This is the mistake everybody makes once, it is invisible for a year, and it surfaces on
     the day of an upgrade, which is the worst possible day.
     ALSO COVER: xsconsole, the local console menu you will use before networking works, and
     the first `xe host-list` — the CLI that this series treats as the runbook.

    ── HANDS-ON (from curriculum.yaml `hands_on`) ──────────────────────────
      koan:process:tool
      koan:capture
      koan file: tests/src/koans/groovy/datazeus/virtualization/series2/_02/InstallingAndDom0Koans.groovy
   ========================================================================== */}

## The situation this lesson prepares you for

{/* BEAT 1 — the concrete moment at work. Never a definition. */}

TODO

## The principle, then how this tool spells it

{/* BEAT 2 — the general idea first, then the syntax. A track teaches a SKILL and DEMONSTRATES it in a tool, never the reverse. */}

TODO

## See it for yourself

{/* BEAT 3 — NON-NEGOTIABLE. The hands-on above, or the reason there isn't one. */}

TODO

## What goes wrong

{/* BEAT 4 — the wrong number or silent failure this prevents, and how you would notice it. */}

TODO
