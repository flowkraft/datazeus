# Build artifacts (built in CI, never committed)

# Anki decks — built from cards/cards.yaml by tools/build-anki.py
**/*.apkg
# Maven (koans) output
**/target/
# rendered videos — published to CDN / website, not git
*.mp4

# Per-machine baseline snapshot `zeus update` uses to detect your edits (never committed)
.internal-donttouch/

# Tooling
node_modules/
__pycache__/
*.pyc
.DS_Store

# Your Data Modeling sandbox: a private copy of Northwind plus the schema.sql you grow
# across Series 1. Created by `zeus practice reset`. Yours, never ours - never committed.
practice/
