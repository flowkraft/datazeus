package datazeus.datamodeling.series1

import spock.lang.Stepwise

/**
 * THE CLASS THE RUNNER PICKS UP for `zeus koans datamodeling series1`. The runner includes only
 * *Koans classes, and the curriculum names the suite NorthwindModelChecks, so the checks live in
 * that abstract base and this concrete class exists only to be found. Nothing goes in here.
 */
@Stepwise
class NorthwindModelChecksKoans extends NorthwindModelChecks {
}
